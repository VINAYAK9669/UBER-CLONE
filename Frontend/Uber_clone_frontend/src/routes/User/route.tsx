import { RouteObject } from "react-router-dom";
import UserLogin from "../../pages/user/UserLogin";

const routes: RouteObject[] = [{ path: "/user/login", element: <UserLogin /> }];

export default routes;
